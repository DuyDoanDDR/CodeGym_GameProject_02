using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tower_Statement : MonoBehaviour
{
    //VirusMovement damageRef;
    //[SerializeField] float HP = 10;

    //private void OnCollisionEnter(Collision collision)
    //{
    //    if (collision.gameObject.CompareTag("Invaders") && HP > 0)
    //    {
    //        HP -= VirusMovement.damage;

    //        if (HP == 0)
    //        {
    //            Time.timeScale = 0;
    //            Debug.Log("GameOver!");
    //        }
    //    }
    //}
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
